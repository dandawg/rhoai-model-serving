apiVersion: batch/v1
kind: Job
metadata:
  name: download-qwen3-vl-embedding-2b
  namespace: demo
  labels:
    app.kubernetes.io/name: qwen3-vl-embedding-2b
    app.kubernetes.io/part-of: rhoai-demos
    app.kubernetes.io/component: model-download
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: qwen3-vl-embedding-2b
        app.kubernetes.io/component: model-download
    spec:
      serviceAccountName: model-downloader
      restartPolicy: OnFailure
      containers:
        - name: download
          image: registry.redhat.io/ubi9/python-311:9.7
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - |
              set -e
              echo "======================================"
              echo "Downloading Qwen3-VL-Embedding-2B model"
              echo "======================================"
              
              echo "Installing HuggingFace CLI..."
              pip install --no-cache-dir huggingface_hub
              
              echo "Model: ${MODEL_ID}"
              echo "Target: ${STORAGE_PATH}"
              echo ""              
              # Token is optional for public models but recommended
              if [ -f /secrets/huggingface/token ]; then
                export HUGGING_FACE_HUB_TOKEN=$(cat /secrets/huggingface/token)
                echo "Using HuggingFace token for authentication"
              else
                echo "WARNING: No HuggingFace token found - download may fail for gated models"
              fi
              
              echo "Starting model download..."
              python3 - <<EOF
              from huggingface_hub import snapshot_download
              import os
              
              model_id = os.environ.get('MODEL_ID')
              storage_path = os.environ.get('STORAGE_PATH')
              token = os.environ.get('HUGGING_FACE_HUB_TOKEN')
              
              print(f"Downloading {model_id} to {storage_path}")
              snapshot_download(
                  repo_id=model_id,
                  local_dir=storage_path,
                  local_dir_use_symlinks=False,
                  token=token if token else None
              )
              print("Download completed successfully!")
              EOF
              
              echo "Verifying download..."
              ls -lah ${STORAGE_PATH}
              
              echo "Model download complete!"
          env:
            - name: MODEL_ID
              valueFrom:
                configMapKeyRef:
                  name: qwen3-vl-embedding-2b-metadata
                  key: model-id
            - name: STORAGE_PATH
              value: "/models/Qwen3-VL-Embedding-2B"
            - name: HUGGINGFACE_URL
              valueFrom:
                configMapKeyRef:
                  name: qwen3-vl-embedding-2b-metadata
                  key: huggingface-url
          volumeMounts:
            - name: model-storage
              mountPath: /models
            - name: huggingface-token
              mountPath: /secrets/huggingface
              readOnly: true
          resources:
            requests:
              cpu: "2"
              memory: 8Gi
            limits:
              cpu: "4"
              memory: 16Gi
      volumes:
        - name: model-storage
          persistentVolumeClaim:
            claimName: qwen3-vl-embedding-2b-model-storage
        - name: huggingface-token
          secret:
            secretName: huggingface-token
            optional: true
